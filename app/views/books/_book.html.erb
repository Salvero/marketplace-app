<div class="col-sm-3">
  <div class="thumbnail">
		
		<%= link_to book_path(book) do %>
    <%= image_tag book.image.url(:original), class: "img-responsive" %>
    <% end %>
<!--     <%= book.image.url(:original) %> -->
    <div class="caption">
    <p><strong><%= link_to book.name.upcase, book_path(book) %></strong></p>
    <p>$<%= book.price %></p>
    </div>
  </div>
</div>